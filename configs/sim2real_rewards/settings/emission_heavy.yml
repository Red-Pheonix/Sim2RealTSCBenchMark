sim2real:
  reward_components: [emission]   # sumo-only; absent in sim, only addressable via correlated proxies
  true_reward:
    queue: 0.5
    emission: 2.0
  sim_episodes: 300
  real_episodes: 100
